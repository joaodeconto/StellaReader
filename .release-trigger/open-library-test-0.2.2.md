# StellaReader v0.2.2 test build

Adds an independent Open Library JSON search under the app menu. The screen reports HTTP status, latency, Dio error type, response status, message, and underlying socket/TLS error so network failures can be isolated from OPDS parsing.
